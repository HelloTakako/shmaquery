{
  "name": "shmaquery",
  "version": "1.0.0",
  "description": "Litter than jQuery",
  "main": "index.js",
  "scripts": {
    "build": "rm -rf dist && mkdir dist && uglifyjs src/* -o dist/shmaquery.min.js --compress --mangle",
    "test": "mocha --reporter nyan --recursive --require chai/register-expect",
    "test:puppeteer": "mocha test/puppeteer-test.js --reporter nyan --recursive --require chai/register-expect --require chai-match"
  },
  "repository": {
    "type": "git",
    "url": "\"type\": \"git\","
  },
  "keywords": [
    "jQuery",
    "shmaquery",
    "JavaScript"
  ],
  "author": "\"\"",
  "license": "ISC",
  "devDependencies": {
    "chai": "^4.2.0",
    "chai-match": "^1.1.1",
    "mocha": "^6.1.4",
    "puppeteer": "^1.14.0",
    "uglify-js": "^3.5.4"
  }
}
